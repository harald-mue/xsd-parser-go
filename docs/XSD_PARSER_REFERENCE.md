# Inspiration

This project is inspired by [`xsd-parser`](https://github.com/Bergmann89/xsd-parser).

The original `xsd-parser` project provided valuable design ideas for pipeline
stages, semantic modeling, and XSD feature coverage. This Go implementation is
fully independent and does not depend on the original project at runtime.
